# Pinned to the versions this workshop was built and verified against.
# Newer versions will usually work, but pins keep a classroom reproducible.

# --- retrieval -------------------------------------------------------------
chromadb==1.5.9
sentence-transformers==6.0.0
langchain-text-splitters==1.1.2
pypdf==6.16.2

# --- graph -----------------------------------------------------------------
neo4j==6.3.0

# --- generation (only the notebooks' live cells need this) -----------------
google-genai==2.20.0

# --- notebooks -------------------------------------------------------------
jupyterlab==4.6.3
nbclient==0.11.0

# --- configuration ---------------------------------------------------------
python-dotenv==1.2.1
